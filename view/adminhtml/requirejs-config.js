var config = {
    paths: {
        algoliaAdminBundle: 'Algolia_AlgoliaSearch/algoliaAdminBundle.min',
    },
    // Callback when the node is created
    // onNodeCreated: function (node, config, moduleName, url) {
    //   console.log(
    //     `Creating script tag for module: ${moduleName} with URL: ${url}`
    //   );
    // },
};
